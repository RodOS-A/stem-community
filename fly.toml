app = "stem-discord-bot"
primary_region = "gru"

[build]
  builder = "docker"

[env]
  NODE_ENV = "production"
  DISCORD_DB_PATH = "/data/discord_conversations.db"

[[mounts]]
  source = "discord_data"
  destination = "/data"
